"""Peer-to-peer discovery: signed multicast announcements, WHOIS over mTLS,
and a local registry with failure detection.

Vendored as it was written, with only its imports made relative so VOX
stays a single installable package.
"""
